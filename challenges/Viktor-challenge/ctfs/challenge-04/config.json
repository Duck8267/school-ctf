{
  "id": "challenge-04",
  "title": "Reverse Lookup",
  "description": "Perform a reverse DNS lookup on the IP address '8.8.8.8'. Identify the primary hostname associated with this IP.",
  "points": 100,
  "hints": [
    "Use a reverse DNS lookup tool.",
    "The PTR record contains the hostname.",
    "The flag format is FLAG{hostname}."
  ],
  "flag": "FLAG{dns.google}"
}